# Missing dcterms:description on Dataset

PREFIX dcat:    <http://www.w3.org/ns/dcat#>
PREFIX dcterms: <http://purl.org/dc/terms/>

SELECT ?distribution
WHERE {
    ?distribution a dcat:Distribution
    FILTER NOT EXISTS { ?distribution dcat:accessURL ?url } 
}
